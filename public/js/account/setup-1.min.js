const setupActions=async event=>{if("step1"==event.currentTarget.id){const json=await Api.post_async("/v1/setup-account",[{prop:"alias",value:document.querySelector('[name="account_alias"]').value},{prop:"default_role_id",value:document.querySelector('[name="default_role_id"]').value}]);if(toast(json.status,json.message),"success"==json.status)return void setTimeout((()=>{window.location.href="/account/setup/2"}),2e3)}};document.addEventListener("DOMContentLoaded",(async()=>{sidebar(),livetime(),setInterval(livetime,1e3),document.querySelectorAll("select").forEach((el=>{new Choices(el,{searchEnabled:!0})}));for await(const el of document.querySelectorAll("button"))el.addEventListener("click",setupActions,!1),el.addEventListener("touchstart",setupActions,!!supportsPassive&&{passive:!0})}),!1);